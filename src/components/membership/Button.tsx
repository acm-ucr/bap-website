const Button = () => {
  return (
    <div className="flex justify-center">
      <button className="rounded-md bg-bap-red-200 px-6 py-2 font-sans text-white transition-all hover:bg-red-800 active:bg-red-900">
        MEMBERSHIP FORM
      </button>
    </div>
  );
};

export default Button;
